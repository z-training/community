Lab 1: Data Passing — Message queue
===================================

Improvements
************

- In this version we use a timer instead of a thread to produce data.
- We added another consumer thread that uses the same entry function. They use
  the argument to behavior differently.

